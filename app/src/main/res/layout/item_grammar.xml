<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="16dp"
    app:cardCornerRadius="24dp"
    app:cardElevation="0dp"
    app:strokeColor="#EEEEEE"
    app:strokeWidth="1dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="24dp"
        android:background="@color/bg_surface">

        <ImageView
            android:id="@+id/iv_timer_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_timer"
            android:padding="12dp"
            android:background="@drawable/bg_circle_orange_light" />

        <TextView
            android:id="@+id/tv_popular_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:background="@drawable/bg_tag_yellow"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"
            android:text="RECOMMEND"
            android:textColor="@color/bg_surface"
            android:textSize="10sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_grammar_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/iv_timer_icon"
            android:layout_marginTop="24dp"
            android:text="Present Tense"
            android:textColor="@color/text_primary"
            android:textSize="24sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_grammar_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/tv_grammar_title"
            android:layout_marginTop="12dp"
            android:text="The foundation of all conversation. Learn -ar, -er, and -ir verb conjugations in their most common forms."
            android:textColor="#666666"
            android:textSize="16sp"
            android:lineSpacingExtra="4dp"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tv_grammar_description"
            android:layout_marginTop="24dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_start_lesson"
                android:layout_width="wrap_content"
                android:layout_height="56dp"
                android:backgroundTint="@color/bg_primary"
                android:text="Start Lesson"
                android:textAllCaps="false"
                android:textColor="@color/text_secondary"
                android:textSize="16sp"
                android:textStyle="bold"
                app:cornerRadius="28dp"
                android:paddingHorizontal="32dp"/>

            <TextView
                android:id="@+id/tv_duration"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:text="15 mins"
                android:textColor="#999999"
                android:textSize="14sp" />
        </LinearLayout>

        <!-- Watermark-like background icon -->
        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:layout_marginEnd="-20dp"
            android:layout_marginBottom="-20dp"
            android:src="@drawable/ic_translate_watermark"
            android:alpha="0.05" />

    </RelativeLayout>
</com.google.android.material.card.MaterialCardView>
